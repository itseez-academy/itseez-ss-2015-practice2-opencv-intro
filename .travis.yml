language: cpp
compiler:
  - gcc
  - clang
sudo: false
addons:
  apt:
    packages:
    - libopencv-dev
before_script:
  - mkdir ./build
  - cd ./build
script:
  - cmake -DCMAKE_BUILD_TYPE=Debug ..
  - make -j
  - cd ./bin
  - ./practice2_test
notifications:
  email: false
